# v2.0.1 Beta 交互优化 To-Do（2026-03-08）

这份待办只覆盖当前边界内的事：

- 个人 Telegram 值守
- `admin_chat` 驾驶舱与命令反馈
- 单账号 / 多账号文字交互
- 风控 / 策略 / 学习 / 任务 / 版本查询

明确不扩展到：

- 网页后台
- 平台化运营系统
- 自动策略调整
- 自动学习转正 / 自动回滚
- 多角色权限系统

---

## A. 已完成收口

- [x] 启动后自动发送 `Admin 驾驶舱`
- [x] 命令收件 / 业务入站 / 消息出站写入 JSON 交互日志
- [x] `model` / `apikey` 命令统一回 `admin_chat`
- [x] 手动 `watch*` 查询统一回 `admin_chat`
- [x] 修复 Windows 下 `ver` 命令编码崩溃
- [x] `help` 改成摘要版，并补 `help all`
- [x] 驾驶舱收成“账号 / 状态 / 策略 / 任务 / 资金 / 收益”摘要头部
- [x] `dashboard` 把“局面 / 轮次 / 轮廓”改成更易懂的“当前局 / 执行档”等表述
- [x] `learn` / `policy` / `ver` 面板继续压缩，减少数据库快照味
- [x] `model` 命令统一成模型卡样式
- [x] 暂停 / 恢复 / 下单 / 结算卡的文案结构继续统一
- [x] `task` / `pkg` 总览命令提示拆成多行
- [x] `doctor` / `watch alerts` 最近异常改成多行显示
- [x] 成功启动后清理历史 `startup*` 异常
- [x] 修复 `fund_pause_notified` 残留导致的资金暂停误报
- [x] 自动化回归达到 `184 passed`

---

## B. P1 待办

### B1. 真实下注事件卡闭环
- [ ] 用单账号最小风险方式验证一次真实 `bet_on -> 下单卡 -> settle -> 结算卡 -> 驾驶舱刷新` 全链路。
- [ ] 把真实事件链路的收件、出件、关键状态字段继续写入 JSON 日志，形成可回放证据。
- [ ] 明确区分“命令反馈消息”和“盘口事件消息”，避免后续再把测试方法搞混。

验收标准：

- 一次真实下注前后，管理员 chat 至少能看到“下注反馈 + 结算反馈 + 驾驶舱变化”。
- 交互日志能完整对上时间顺序。

### B2. M3 手卡标准化
- [x] 统一下单卡样式。
- [x] 统一结算卡样式。
- [ ] 统一自动暂停卡 / 自动恢复卡样式。
- [ ] 补“跳过下注 / 观望 / 阻断”卡片。

验收标准：

- 你在聊天窗口往上翻时，能看出“这一轮发生了什么”，而不是只看到驾驶舱大面板。

### B3. 命令帮助继续压缩
- [ ] 在不加新命令前提下，把默认 `help` 再压缩一轮。
- [ ] 如果后续仍偏长，再评估是否拆 `help watch/task/policy/learn`。

验收标准：

- 默认 `help` 控制在 25 到 35 行左右，且一屏内能看出“先看什么 / 常用什么 / 去哪查”。
- 深入帮助如果要扩展，优先保证默认帮助不反弹成超长文本。

### B4. 启动与告警语义再校准
- [ ] 明确“启动提醒 / 启动自检摘要 / watch 主动播报 / admin_chat 驾驶舱”之间的发送规则。
- [ ] 避免启动时多处重复提醒相同信息。
- [ ] 给“当前风险”与“历史已恢复异常”增加更明确的区分。

验收标准：

- 启动后 10 秒内，你能看到最重要的信息，但不会连续收到内容重复的多条消息。

---

## C. P2 待办

### C1. 驾驶舱信息再压缩
- [x] 重新梳理“局面 / 轮次 / 轮廓”这些词的用户可读性。
- [x] 把技术味较重的字段压缩成更像业务状态的表达。
- [x] 对手机端容易撑宽的行继续拆分。

验收标准：

- 不看代码也能大致理解当前状态。
- 驾驶舱单行尽量控制在 50 到 60 字以内，少数概览行除外。

### C2. `learn` 与 `policy` 面板精简
- [x] `learn` 总览减少“空值字段墙”。
- [x] `policy` 总览增加“现在该不该动”的一句话。
- [ ] `ver` 面板考虑增加 `ver all`，把默认版再压缩一轮。

验收标准：

- `learn` 和 `policy` 默认版更像摘要，不像数据库快照。

### C3. 任务 / 任务包交互继续优化
- [ ] `task` 总览增加“下一步建议命令”。
- [ ] `pkg` 总览增加“当前无任务包时的推荐动作”。
- [ ] `task list` / `pkg list` 超过 5 项时考虑分页或压缩。

验收标准：

- 当任务为空时，不需要翻文档也知道下一步该怎么建。

---

## D. P3 待办

### D1. 高风险运维命令专项验证
- [ ] `update`
- [ ] `reback`
- [ ] `restart`
- [ ] `apikey set/add/del/test`

要求：

- 单独做隔离验证，不和实时实盘混跑。
- 每条命令都要有“执行前状态 / 执行中提示 / 执行后反馈 / 失败时反馈”。

### D2. 文档继续补齐
- [ ] 在总技术文档里补一节“管理员命令真实交互回路”。
- [ ] 把 `interaction_journal.jsonl` 的字段说明单独列出来。
- [ ] 为“真实事件卡测试”补一份专项说明。

---

## E. 推荐执行顺序

1. 先做 `B1 真实下注事件卡闭环`
2. 再做 `B2 M3 手卡标准化`
3. 再做 `B4 启动与告警语义再校准`
4. 然后做 `B3 help 分域`
5. 最后再补 `C` 和 `D`

---

## F. 当前建议

如果明天继续推进，不建议先去加新功能。

最值钱的顺序是：

1. 继续用 `tim` 做最小风险真实事件闭环验证。
2. 把真实“下注 / 结算 / 暂停 / 恢复”卡片打磨好。
3. 确认这些卡片和驾驶舱一起使用时，不会刷屏，不会语义打架。

做到这一步，`v2.0.1-beta` 才更接近真正可长期盯的实盘测试版。
